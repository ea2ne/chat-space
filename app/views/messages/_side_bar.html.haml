.side-bar
  .top-items
    %h3.name
      = current_user.name
    %ul.icons
      %li.left__icon
        = link_to '#' do
          = icon('fas', 'edit')
      %li.right__icon
        = link_to edit_user_path(current_user) do
          = icon('fas', 'cog')
  .group-list
    .tech-logo
      <すいかバー>
    .side-text
      <あずきバー>